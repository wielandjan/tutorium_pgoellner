
public class UFO extends FlyingMachine {
	public UFO(String name, double maxSpeed, double maxAltitude, double price) {
		setName(name);
		setMaxSpeed(maxSpeed);
		setMaxAltitude(maxAltitude);
		setPrice(price);
	}
}
