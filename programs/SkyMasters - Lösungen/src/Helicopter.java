
public class Helicopter extends FlyingMachine {
	public Helicopter(String name, double maxSpeed, double maxAltitude, double price) {
		setName(name);
		setMaxSpeed(maxSpeed);
		setMaxAltitude(maxAltitude);
		setPrice(price);
	}
}
