
public class Airship extends FlyingMachine {
	public Airship(String name, double maxSpeed, double maxAltitude, double price) {
		setName(name);
		setMaxSpeed(maxSpeed);
		setMaxAltitude(maxAltitude);
		setPrice(price);
	}
}
