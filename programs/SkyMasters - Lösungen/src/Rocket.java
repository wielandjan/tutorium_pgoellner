
public class Rocket extends FlyingMachine {
	public Rocket(String name, double maxSpeed, double maxAltitude, double price) {
		setName(name);
		setMaxSpeed(maxSpeed);
		setMaxAltitude(maxAltitude);
		setPrice(price);
	}
}
